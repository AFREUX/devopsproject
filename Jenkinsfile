pipeline {
    agent any

    stages {
        stage('Test Webhook') {
            steps {
                echo 'Webhook triggered successfully!'
                echo 'This is a simple test to verify Jenkins is executing the pipeline.'
            }
        }
    }
}
